spring.application.name=reservation-client
server.port=${server_port}
spring.cloud.config.uri=${config_server_uri}
spring.cloud.stream.bindings.output=reservations
eureka.client.serviceUrl.defaultZone=http://${eureka_hostname}:${eureka_port:80}/eureka